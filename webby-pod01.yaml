apiVersion: v1
kind: Pod
metadata:
  name: webservice01
spec:
  containers:
  - name: webby
    image: reg.alta3.com/5296-sam-webby
    ports:
    - name: web
      containerPort: 8888
      protocol: TCP
